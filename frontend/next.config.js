/** @type {import('next').NextConfig} */
const withPWA = require('next-pwa')({
  dest: 'public',
  register: true,
  skipWaiting: true,
  disable: process.env.NODE_ENV === 'development'
});

const nextConfig = {
  output: 'export',
  trailingSlash: true,
  images: {
    unoptimized: true
  },
  basePath: process.env.NODE_ENV === 'production' ? '/MoneyManager' : '',
  assetPrefix: process.env.NODE_ENV === 'production' ? '/MoneyManager/' : '',
};

module.exports = withPWA(nextConfig);
